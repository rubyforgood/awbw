<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <style>
      body { font-family: Arial, sans-serif; color: #333; }
      h1 { color: #2c3e50; }
      p { margin: 0.5em 0; }
      ul { margin: 0.5em 0 1em 1.5em; }
      a { color: #1a73e8; text-decoration: none; }
      .footer { margin-top: 2em; font-size: 0.85em; color: #888; }
      .section { margin-bottom: 1.5em; }
      .section h2 { font-size: 1.1em; color: #2c3e50; margin-bottom: 0.5em; }
  </style>
</head>
<body>
<h1>New <%= @type %> Submission</h1>

<p>Hello,</p>

<p>
  A new <strong><%= @type %></strong> was submitted by <strong><%= @user.name %></strong>.
</p>

<% if @report.present? %>
  <div class="section">
    <h2>Report Details</h2>
    <ul>
      <li><strong>Report Type:</strong> <%= @report.type %></li>
      <% if @noticeable.respond_to?(:windows_type) %>
        <li><strong>Target:</strong> <%= @noticeable.windows_type.name %></li>
      <% end %>
      <li><strong>Submitted at:</strong> <%= Time.current.strftime("%B %d, %Y %l:%M %p") %></li>
    </ul>
  </div>

  <% if @attachments.present? %>
    <div class="section">
      <h2>Attachments</h2>
      <ul>
        <% @attachments.each do |file| %>
          <li>
            <%= file.file_file_name %> (<%= file.file_content_type %>)
          </li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <% if @quotes.present? %>
    <div class="section">
      <h2>Quotes</h2>
      <ul>
        <% @quotes.each do |quote| %>
          <li><%= quote.quote %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <% if @answers.present? %>
    <div class="section">
      <h2>Form Answers</h2>
      <ul>
        <% @answers.each do |answer| %>
          <li><strong><%= answer.name %>:</strong> <%= answer.answer %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
<% end %>

<p>
  <%= link_to "View Submission", workshop_log_url(@report) if @report.present? %>
</p>

<div class="footer">
  This is an automated message from AWBW.
</div>
</body>
</html>
