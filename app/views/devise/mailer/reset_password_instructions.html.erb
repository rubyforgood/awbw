<table width="100%" cellpadding="0" cellspacing="0" style="margin-bottom: 24px;">
  <tr>
    <td align="center">
      <a href="<%= root_url %>" target="_blank">
        <img
          src="<%= asset_url("logo-color.png") %>"
          alt="<%= @organization_name %>"
          width="160"
          style="display: block; margin-bottom: 32px;"
          />
      </a>
    </td>
  </tr>
</table>

<p>Hello <%= @resource.devise_email_name %>!</p>

<p>
  We received a request to reset the password for your
  <strong><%= @organization_name %> portal account</strong>.
</p>

<p>
  This request was made using the email address associated with your account.
  The link below will expire in
  <%= distance_of_time_in_words(Devise.reset_password_within) %>
  and can only be used once.
</p>

<p><%= link_to "Reset your password", edit_password_url(@resource, reset_password_token: @token) %></p>

<p>
  If you did not request this change, you can safely ignore this email.
  No changes will be made unless you use the link above.
</p>

<p>
  If you have any concerns regarding this email, please visit
  <%= link_to "our contact page", contact_us_url %> to connect with staff.
</p>

<p style="margin-top: 24px; font-size: 12px; color: #666;">
  This message was sent by <%= @organization_name %> Team
</p>
