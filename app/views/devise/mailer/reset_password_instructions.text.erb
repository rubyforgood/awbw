Hello <%= @resource.devise_email_name %>,

We received a request to reset the password for your <%= @organization_name %> portal account.

This request was made using the email address associated with your account.
The reset link below will expire in <%= distance_of_time_in_words(Devise.reset_password_within) %> and can only be used once.

To reset your password, open this link:

<%= edit_password_url(@resource, reset_password_token: @token) %>

If you did not request this change, you can safely ignore this email.
No changes will be made unless you use the link above.

If you have any concerns regarding this email, please visit our contact page to connect with staff:
<%= contact_us_url %>

Kindly,
<%= @organization_name %> Team
