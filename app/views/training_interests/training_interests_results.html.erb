<%= turbo_frame_tag :training_interests_results do %>
  <div class="bg-white border border-gray-200 rounded-xl shadow-sm overflow-hidden blur-on-submit">
    <% if @training_interests.any? %>
      <% total = @training_interests.respond_to?(:total_entries) ? @training_interests.total_entries : @training_interests.size %>
      <div class="flex items-center justify-between px-6 py-4 border-b border-gray-100">
        <p class="text-sm text-gray-500"><%= total %> interest<%= "s" if total != 1 %></p>
      </div>

      <div class="overflow-x-auto animate-fade">
        <table class="w-full border-collapse border border-gray-200">
          <thead class="bg-gray-100">
            <tr>
              <th class="px-4 py-2 text-left text-sm font-semibold text-gray-700">Person</th>
              <th class="px-4 py-2 text-left text-sm font-semibold text-gray-700">Interested in</th>
              <th class="px-4 py-2 text-left text-sm font-semibold text-gray-700">Status</th>
              <th class="px-4 py-2 text-left text-sm font-semibold text-gray-700">Training registrations</th>
              <th class="px-4 py-2 text-left text-sm font-semibold text-gray-700">Source</th>
              <th class="px-4 py-2 text-left text-sm font-semibold text-gray-700">Expressed</th>
              <th class="px-4 py-2"></th>
            </tr>
          </thead>

          <tbody class="divide-y divide-gray-200">
            <% @training_interests.each do |interest| %>
              <% interest = interest.decorate %>
              <tr class="hover:bg-gray-50 transition-colors duration-150 align-top">
                <td class="px-4 py-2 text-sm text-gray-800">
                  <%= link_to interest.person.full_name, person_path(interest.person), class: "text-blue-600 hover:text-blue-800 hover:underline", data: { turbo_frame: "_top" } %>
                </td>

                <td class="px-4 py-2 text-sm text-gray-800">
                  <% if interest.general? %>
                    <span class="inline-flex items-center gap-1.5 text-gray-500">
                      <i class="fa-solid fa-layer-group text-xs"></i>
                      <%= interest.target_label %>
                    </span>
                  <% else %>
                    <%= interest.target_label %>
                  <% end %>
                </td>

                <td class="px-4 py-2 text-sm"><%= interest.status_badge %></td>

                <td class="px-4 py-2 text-sm text-gray-800">
                  <% registrations = interest.person_facilitator_training_registrations %>
                  <% if registrations.any? %>
                    <ul class="space-y-1">
                      <% registrations.each do |registration| %>
                        <li class="flex flex-wrap items-center gap-2">
                          <%= link_to registration.event.title, edit_event_registration_path(registration), class: "text-blue-600 hover:text-blue-800 hover:underline", data: { turbo_frame: "_top" } %>
                          <span class="text-xs text-gray-500">(<%= registration.attendance_status_label %>)</span>
                        </li>
                      <% end %>
                    </ul>
                  <% else %>
                    <span class="text-gray-400">—</span>
                  <% end %>
                </td>

                <td class="px-4 py-2 text-sm text-gray-500"><%= interest.source.presence || "—" %></td>

                <td class="px-4 py-2 text-sm text-gray-500 text-nowrap"><%= interest.expressed_at.strftime("%B %d, %Y") %></td>

                <td class="px-4 py-2 text-left text-sm font-medium text-nowrap">
                  <% link_class = "inline-flex items-center gap-1.5 rounded-md px-2.5 py-1 text-xs font-medium text-gray-600 transition-colors hover:bg-gray-100 hover:text-gray-900 cursor-pointer" %>
                  <%= link_to edit_training_interest_path(interest), class: link_class, data: { turbo_frame: "_top" } do %>
                    <i class="fa-solid fa-pen text-[0.7rem] text-gray-400"></i>
                    Edit
                  <% end %>
                  <%= button_to training_interest_path(interest), method: :delete,
                        class: "inline-flex items-center gap-1.5 rounded-md px-2.5 py-1 text-xs font-medium text-gray-600 transition-colors hover:bg-red-50 hover:text-red-700 cursor-pointer",
                        data: { turbo_confirm: "Remove this interest?", turbo_frame: "_top" } do %>
                    <i class="fa-solid fa-trash-can text-[0.7rem]"></i>
                    Remove
                  <% end %>
                </td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    <% else %>
      <p class="text-gray-500 text-center py-8">No training interest matches your search.</p>
    <% end %>
  </div>

  <% if @training_interests.respond_to?(:total_pages) && @training_interests.total_pages > 1 %>
    <div class="pagination flex justify-center mt-6"><%= tailwind_paginate @training_interests %></div>
  <% end %>
<% end %>
