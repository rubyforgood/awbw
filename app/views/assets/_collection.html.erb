<div id="asset_collection" class="flex flex-wrap gap-2 p-3 m-2 bg-white my-5 rounded-md">
  <% owner.assets.each do |asset| %>
    <% next if asset.type == "RichTextAsset" %>

    <%= render "assets/card", asset: asset, owner: owner %>
  <% end %>
</div>
