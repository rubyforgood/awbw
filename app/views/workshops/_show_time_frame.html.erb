<div class="flex flex-wrap divide-x divide-gray-300 border border-gray-300 rounded-lg overflow-hidden">
  <% unless workshop.time_intro.to_i == 0 %>
    <div class="flex-1 p-4 text-center">
      <h3 class="text-sm font-semibold text-gray-700">
        <%= language == "english" ? "Intro" : "Introducción" %>
      </h3>
      <div class="text-sm text-gray-600">
        <span><%= workshop.time_intro.to_i %></span> <span>min</span>
      </div>
    </div>
  <% end %>

  <% unless workshop.time_demonstration.to_i == 0 %>
    <div class="flex-1 p-4 text-center">
      <h3 class="text-sm font-semibold text-gray-700">
        <%= language == "english" ? "Demonstration" : "Demostración" %>
      </h3>
      <div class="text-sm text-gray-600">
        <span><%= workshop.time_demonstration.to_i %></span> <span>min</span>
      </div>
    </div>
  <% end %>

  <% unless workshop.time_opening.to_i == 0 %>
    <div class="flex-1 p-4 text-center">
      <h3 class="text-sm font-semibold text-gray-700">
        <%= language == "english" ? "Opening" : "Apertura" %>
      </h3>
      <div class="text-sm text-gray-600">
        <span><%= workshop.time_opening.to_i %></span> <span>min</span>
      </div>
    </div>
  <% end %>

  <% unless workshop.time_opening_circle.to_i == 0 %>
    <div class="flex-1 p-4 text-center">
      <h3 class="text-sm font-semibold text-gray-700">
        <%= language == "english" ? "Opening Circle" : "Círculo de Apertura" %>
      </h3>
      <div class="text-sm text-gray-600">
        <span><%= workshop.time_opening_circle.to_i %></span> <span>min</span>
      </div>
    </div>
  <% end %>

  <% unless workshop.time_warm_up.to_i == 0 %>
    <div class="flex-1 p-4 text-center">
      <h3 class="text-sm font-semibold text-gray-700">
        <%= language == "english" ? "Warm Up" : "Comenzando" %>
      </h3>
      <div class="text-sm text-gray-600">
        <span><%= workshop.time_warm_up.to_i %></span> <span>min</span>
      </div>
    </div>
  <% end %>

  <% unless workshop.time_creation.to_i == 0 %>
    <div class="flex-1 p-4 text-center">
      <h3 class="text-sm font-semibold text-gray-700">
        <%= language == "english" ? "Creation" : "Creación" %>
      </h3>
      <div class="text-sm text-gray-600">
        <span><%= workshop.time_creation.to_i %></span> <span>min</span>
      </div>
    </div>
  <% end %>

  <% unless workshop.time_closing.to_i == 0 %>
    <div class="flex-1 p-4 text-center">
      <h3 class="text-sm font-semibold text-gray-700">
        <%= language == "english" ? "Closing" : "Clausura" %>
      </h3>
      <div class="text-sm text-gray-600">
        <span><%= workshop.time_closing.to_i %></span> <span>min</span>
      </div>
    </div>
  <% end %>

  <div class="flex-1 p-4 text-center bg-gray-50">
    <h3 class="text-sm font-semibold text-gray-700">
      <%= language == "english" ? "Total" : "Total" %>
    </h3>
    <div class="text-sm font-bold text-gray-800">
      <%= workshop.time_frame_total %>
    </div>
  </div>
</div>