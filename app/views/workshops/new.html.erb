<div class="max-w-7xl mx-auto px-4 py-8">
  <div class="max-w-5xl mx-auto <%= DomainTheme.bg_class_for(:workshops, intensity: 100) %>
    border border-gray-200 rounded-xl shadow p-6">
    <!-- Header + View Button -->
    <div class="flex items-center justify-between mb-4">
      <h1 class="text-2xl font-semibold text-gray-900">New Workshop</h1>
    </div>

      <!-- Form -->
    <div class="bg-white rounded p-6">
      <%= render "form", workshop: @workshop, url: '/workshops' %>
    </div>
  </div>
</div>
