<div class="relative w-full h-64 md:h-80 lg:h-96 overflow-hidden">
  <!-- Background image -->
  <%= render "assets/display_image",
             resource: workshop,
             width: "full", height: "x",
             link: false,
             link_to_object: false,
             file: workshop.display_image %>

  <!-- Bottom blurred overlay -->
  <div
    class="absolute inset-x-0 bottom-0 h-1/2
           bg-gradient-to-t from-black/70 via-black/40 to-transparent
           backdrop-blur-sm">
  </div>

  <!-- Title + meta -->
  <div class="inner-hero absolute bottom-0 left-0 p-6 md:p-10">
    <h1 class="text-3xl md:text-4xl font-bold text-white drop-shadow-lg">
      <%= workshop.title %>
      <% if workshop.inactive %>
        <span class="text-red-300 text-lg">(Hidden)</span>
      <% end %>
    </h1>

    <div class="flex flex-wrap gap-4 text-sm text-gray-200 mt-3">
      <span>Author: <%= workshop.author %></span>
      <span>Category: <%= workshop.windows_type&.short_name %></span>
      <span>Added: <%= workshop.date %></span>
    </div>
  </div>
</div>
