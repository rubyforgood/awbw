<div class="bg-white shadow-sm rounded-xl p-4 hover:shadow-md transition">

  <%= link_to community_news_path(news), class: "flex flex-row items-start gap-4" do %>

    <!-- LEFT: TITLE + BODY -->
    <div class="flex-1">
      <h3 class="text-lg font-semibold text-gray-900 leading-tight">
        <%= news.title %>
      </h3>

      <p class="text-gray-600 text-sm mt-1 line-clamp-3">
        <%#= strip_tags(news.body) %>
      </p>

      <p class="text-xs text-gray-400 mt-2">
        <%#= news.created_at.to_date.to_s(:long) %>
      </p>
    </div>

    <!-- RIGHT: BOOKMARK -->
    <div class="shrink-0 pt-1">
      <%= render "bookmarks/editable_bookmark_icon", resource: news.object %>
    </div>

  <% end %>
</div>
