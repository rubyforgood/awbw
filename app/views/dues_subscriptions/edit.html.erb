<% content_for(:page_bg_class, "admin-only bg-blue-100") %>

<div class="mx-auto max-w-2xl bg-white border border-gray-200 rounded-xl shadow p-4 sm:p-6">
  <div class="flex flex-wrap items-center justify-between gap-2 mb-6">
    <%= link_to person_dues_subscriptions_path(@person), class: "text-sm text-gray-500 hover:text-gray-700" do %>
      <i class="fa-solid fa-arrow-left text-xs"></i> Dues
    <% end %>
  </div>

  <h1 class="text-2xl font-bold mb-1">Dues rate</h1>
  <p class="text-sm text-gray-500 mb-6"><%= @person.full_name %></p>

  <%= simple_form_for @dues_subscription do |f| %>
    <%= render "shared/errors", resource: f.object %>

    <%= f.input :rate_dollars,
          label: "Rate ($ per year)",
          required: false,
          hint: "Leave blank for the standard rate (#{dollars_from_cents(Dues::ANNUAL_COST_CENTS)}). Applies to future dues years only — years already created keep the price they were billed at.",
          input_html: { min: 0, step: 0.01, placeholder: "Standard", class: "bg-white" } %>

    <div class="flex items-center gap-3 border-t border-gray-200 pt-6 mt-6">
      <div class="ml-auto flex items-center gap-3">
        <%= link_to "Cancel", person_dues_subscriptions_path(@person), class: "btn btn-secondary-outline" %>
        <%= f.button :submit, "Save rate", class: "btn btn-primary" %>
      </div>
    </div>
  <% end %>
</div>
