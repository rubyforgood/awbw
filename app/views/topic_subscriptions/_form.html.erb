<% field_class = "w-full rounded-lg border border-gray-300 px-3 py-2 text-gray-800 shadow-sm focus:border-blue-500 focus:ring focus:ring-blue-200 focus:outline-none" %>
<% label_class = "block text-sm font-medium text-gray-700 mb-1" %>

<%= form_with model: @topic_subscription, class: "space-y-5" do |f| %>
  <% if @topic_subscription.errors.any? %>
    <div class="rounded-lg border border-red-200 bg-red-50 px-4 py-3 text-sm text-red-700">
      <ul class="list-disc list-inside space-y-1">
        <% @topic_subscription.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= f.label :person_id, "Person", class: label_class %>
    <%= f.select :person_id,
      options_for_select(Person.where(id: @topic_subscription.person_id).map { |p| [ p.full_name, p.id ] }, @topic_subscription.person_id),
      { include_blank: true, prompt: "Search for a person" },
      class: field_class,
      data: { controller: "remote-select", remote_select_model_value: "person" } %>
  </div>

  <div>
    <%= f.label :topic, class: label_class %>
    <%= f.select :topic,
      options_for_select(TopicSubscription::TOPICS.map { |t| [ TopicSubscription::TOPIC_LABELS.fetch(t, t.humanize), t ] }, @topic_subscription.topic),
      {},
      class: field_class %>
  </div>

  <div>
    <%= f.label :interested_event_id, "Event (optional)", class: label_class %>
    <%= f.select :interested_event_id,
      options_for_select(Event.where(id: @topic_subscription.interested_event_id).map { |e| [ e.title, e.id ] }, @topic_subscription.interested_event_id),
      { include_blank: "Any — the topic broadly" },
      class: field_class,
      data: { controller: "remote-select", remote_select_model_value: "event" } %>
    <p class="mt-1 text-xs text-gray-500">Leave blank to subscribe to the topic broadly (e.g. all future trainings).</p>
  </div>

  <div>
    <%= f.label :source, class: label_class %>
    <%= f.text_field :source, class: field_class, placeholder: "e.g. Facilitator Training registration, admin" %>
  </div>

  <div>
    <%= f.label :note, "Note (optional)", class: label_class %>
    <%= f.text_area :note, rows: 3, class: field_class %>
  </div>

  <div class="flex gap-2 justify-end">
    <%= link_to "Cancel", topic_subscriptions_path, class: "btn btn-utility-outline" %>
    <%= f.submit submit_label, class: "btn btn-primary" %>
  </div>
<% end %>
