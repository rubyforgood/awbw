<% portal_url = @facilitator ? facilitator_url(@facilitator) : user_url(@user) %>

<%= @notification_type %>

Hello,

<%= @user.full_name %> (<%= @user.email %>) requested a password reset on
<%= Time.current.strftime("%A, %B %-d, %Y at %-l:%M %p %Z") %>.

View their profile:
<%= portal_url %>

<% if @facilitator&.avatar&.file&.attached? %>
  Avatar:
  <%= url_for(@facilitator.avatar.file) %>
<% end %>

--
This is an automated message from AWBW.
